# 2026-08-04

## Time
4h

## What I did

### mini-kvstore
- Added InstallSnapshot RPC (Raft paper section 7)
- Snapshot serialization: dump entire state machine to JSON
- Snapshot restore: follower receives/installs snapshot file
- Auto compaction: trigger snapshot when log exceeds 1000 entries
- Fixed nextIndex tracking for snapshot prefix (logical vs physical index logic)

### mini-sql
- Added UPDATE SET support (parser + engine)
- New UpdateStmt AST node, _exec_update with column type conversion
- Fixed Literal type coercion - number literals in WHERE were strings, causing int vs str comparison failures
- Added test_update.py

### mini-http-server
- Added HTTP cache headers: Last-Modified, ETag, Cache-Control
- 304 Not Modified response for If-Modified-Since conditional requests
- ETag built from mtime + content_length (simple but functional)

### cv-toolkit
- Added img_tools.py: face detection (DNN + HSV fallback), CLAHE enhancement, contour detection, batch processor
- Skin color detection using HSV thresholding as fallback when DNN model not available
- CLAHE: LAB color space, enhance L channel only, merge back

### mini-kvstore (afternoon)
- Added HTTP REST API layer (stdlib http.server, no deps)
- Endpoints: GET /api/get, POST /api/set, POST /api/del, GET /api/keys, GET /api/stats, GET /api/scan
- CORS support, JSON responses
- All 5 repos pushed successfully
