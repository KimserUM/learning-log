# 2026-08-04

## 时间
4h

## 做了什么

### mini-kvstore -- InstallSnapshot RPC
- 实现了Raft论文第7节的快照机制(InstallSnapshot RPC)
- 状态机序列化为JSON快照文件, follower接收后恢复
- 日志超过1000条自动触发compaction
- 修复了snapshot prefix的逻辑索引vs物理索引映射
- 下午加了HTTP REST API层(stdlib http.server, 无外部依赖)
- 接口: GET /api/get, POST /api/set, POST /api/del, GET /api/keys, GET /api/stats, GET /api/scan

### mini-sql -- UPDATE SET支持
- 新增UPDATE SET语句(解析器 + 执行引擎)
- UpdateStmt AST节点, _exec_update含列类型转换
- 修了一个坑: WHERE条件里数字字面量被当字符串, 导致 `id = 1` 永远匹配不上
- 加了test_update.py

### mini-http-server -- HTTP缓存头
- 加了 Last-Modified, ETag, Cache-Control 响应头
- 支持 If-Modified-Since 条件请求, 返回 304 Not Modified
- ETag用文件mtime + content_length拼接

### cv-toolkit -- 图像工具扩展
- img_tools.py: 人脸检测(DNN+HSV肤色回退), CLAHE增强, 轮廓检测, 批量处理
- HSV肤色检测作为不需要模型文件的回退方案
- CLAHE: LAB色彩空间, 只动L通道不动颜色

## 已推送
全部5个仓库都已push成功。
