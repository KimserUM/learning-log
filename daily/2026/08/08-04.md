# 2026-08-04

## Time
3h

## What I did

### mini-kvstore
- Added InstallSnapshot RPC (Raft paper section 7)
- Implemented snapshot serialization: dump entire state machine to JSON
- Added snapshot restore: follower receives/installs snapshot file
- Automatic compaction trigger: take snapshot when log exceeds 1000 entries
- Fixed nextIndex tracking to work with snapshot prefix (logical vs physical index)

### mini-sql
- Added UPDATE SET support
- Parser: new UpdateStmt AST node, parse_update method
- Engine: _exec_update with column type conversion
- Fixed Literal type coercion - number literals in WHERE were strings,
  causing `id = 1` to compare int to str and never match
- Added test_update.py

## Next
- mini-http-server: add caching headers (ETag, Last-Modified, Cache-Control)
