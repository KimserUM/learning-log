# 2026-08-05

## 时间
3h

## 做了什么

### mini-kvstore -- TTL + HTTP API扩展
- store.py已经加了setex/expire/ttl + 后台过期清理(用户之前写的)
- api.py新增三个接口: POST /api/setex, POST /api/expire, POST /api/ttl
- /api/set接口也支持可选的ttl参数
- raft.py的_apply_to_state支持ttl字段, 集群模式下也能正确复制带过期时间的写入
- 跑通全部11个测试

### mini-sql -- 交互式shell
- 加了src/shell.py, `python -m src.shell`启动
- 表格格式化输出(自动列宽, 分隔线)
- 点命令: .tables .help .quit
- 支持SELECT/INSERT/CREATE/DELETE/UPDATE
- 加了test_shell.py

### cv-toolkit -- 分类器benchmark
- benchmark.py: 对比颜色直方图 vs HOG特征
- 多K值对比(1/3/5/7)
- 记录训练/推理耗时
- 合成数据上颜色直方图100%, HOG 25-43%(因为合成图颜色是主信号, 纹理少)
